<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- 배경색 지정 -->
    <stroke android:width="2dp" android:color="@android:color/darker_gray" /> <!-- 테두리 색상과 두께 지정 -->
    <corners android:radius="10dp" /> <!-- 모서리 둥글게 설정 -->
</shape>
